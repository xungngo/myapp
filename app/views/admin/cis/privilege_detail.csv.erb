<% if params[:fco].present? %>
	<% title = "CIS: Detailed Priv Report for Priv #{params[:trans]} and Location #{params[:fco]}" %>
<% else %>
	<% title = "CIS: Detailed Priv Report for Priv #{params[:trans]}" %>
<% end %>
<%= title %>

<% if @cis.length > 0 %>
#,FIRST NAME,MIDDLE INIT,LAST NAME,USER ID,PRIV,RECORD COUNT
<% @cis.each do |cis| %>
<%= cis['currentrow'] %>,<%= cis['firstname'] %>,<%= cis['middleinit'] %>,<%= cis['lastname'] %>,<%= cis['userid'] %>,<%= cis['privilege'] %>,<%= cis['usercount'] %>
<% end %>
<% end %>

Data Import: <%= @data_import.created_at.strftime("%b %Y") %>
Report Generated: <%= DateTime.now.strftime("%-m/%e/%Y %l:%M:%S %p ET") %>
