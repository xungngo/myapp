CIS: Detail Report By Location (<%= params[:fco] %>)

<% if @cis.length > 0 %>
#,NAME,USER ID,REC CNT<% @cistransaction.each do |t| %>,<%= t.name %><% end %>
<% @cis.each do |cis| %>
<%= cis['currentrow'] %>,<%= cis['fullname'] %>,<%= cis['userid'] %>,<%= cis['reccnt'] %><% ct = 0 %><% @cistransaction.each do |t| %>,<%= cis['transaction'][ct][t.name] %><% ct = ct + 1 %><% end %>
<% end %>
<% end %>

Data Import: <%= @data_import.created_at.strftime("%b %Y") %>
Report Generated: <%= DateTime.now.strftime("%-m/%e/%Y %l:%M:%S %p ET") %>
The "REC CNT" column represents the number of times restricted transactions were used in the last year.
