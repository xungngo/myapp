<% if params[:fco].present? %>
	<% title = "TRKS: Detailed Priv Report for Priv #{params[:trans]} and Location #{params[:fco]}" %>
<% else %>
	<% title = "TRKS: Detailed Priv Report for Priv #{params[:trans]}" %>
<% end %>
<%= title %>

<% if @trks.length > 0 %>
#,FIRST NAME,MIDDLE INIT,LAST NAME,USER ID,PRIV,RECORD COUNT
<% @trks.each do |trks| %>
<%= trks['currentrow'] %>,<%= trks['firstname'] %>,<%= trks['middleinit'] %>,<%= trks['lastname'] %>,<%= trks['userid'] %>,<%= trks['privilege'] %>,<%= trks['usercount'] %>
<% end %>
<% end %>

Data Import: <%= @data_import.created_at.strftime("%b %Y") %>
Report Generated: <%= DateTime.now.strftime("%-m/%e/%Y %l:%M:%S %p ET") %>
