TRKS: Detail Report By Location (<%= params[:fco] %>)

<% if @trks.length > 0 %>
#,NAME,USER ID,REC CNT<% @trkstransaction.each do |t| %>,<%= t.name %><% end %>
<% @trks.each do |trks| %>
<%= trks['currentrow'] %>,<%= trks['fullname'] %>,<%= trks['userid'] %>,<%= trks['reccnt'] %><% ct = 0 %><% @trkstransaction.each do |t| %>,<%= trks['transaction'][ct][t.name] %><% ct = ct + 1 %><% end %>
<% end %>
<% end %>

Data Import: <%= @data_import.created_at.strftime("%b %Y") %>
Report Generated: <%= DateTime.now.strftime("%-m/%e/%Y %l:%M:%S %p ET") %>
The "REC CNT" column represents the number of times restricted transactions were used in the last year.

